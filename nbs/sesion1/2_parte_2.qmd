---
format: html
toc: false
---

## 🧠 Qué significa implementar un clasificador

::: {.callout-tip title="Idea clave"}
Entender qué significa implementar un clasificador es el segundo paso para comprender una funcionalidad de clasificación de imágenes como parte de un sistema de software. En esta etapa, el foco **no está en el entrenamiento ni en las matemáticas**, sino en entender **qué es un clasificador**, **qué decisiones implica** y **qué rol cumple dentro de la funcionalidad completa**.
:::

### 🧩 ¿Qué es un clasificador?

Un **clasificador** puede entenderse como una funcionalidad de software que:

- Recibe **datos de entrada**
- Produce una **categoría** como resultado
- Decide a cuál de las clases disponibles **se parecen más esos datos**

Su tarea **no es describir** los datos ni interpretarlos de forma humana, sino **tomar una decisión categórica**.

En el contexto de imágenes, esto significa decidir, por ejemplo, si una imagen:

- Pertenece a una clase u otra  
- Se parece más a un conjunto de ejemplos que a otro  

### 🌤️ El clasificador como problema de predicción

Siguiendo el ejemplo del clima usado anteriormente, un clasificador funciona de forma similar a una persona que, después de observar muchas veces las condiciones del día, decide si el clima será:

- “Bueno”
- “Malo”

El clasificador:

- No calcula grados  
- No explica por qué ocurre el fenómeno  
- Solo observa los datos y **elige la categoría más parecida a lo que ha visto antes**

En el caso de imágenes, el proceso es equivalente: el sistema recibe una imagen nueva y decide si se parece más a los ejemplos de una clase u otra, produciendo así una **predicción categórica**.

### 🏷️ Definir las clases

Implementar un clasificador implica, en primer lugar, **definir las clases** del problema. Esto es una decisión de diseño, no algo automático.

Al definir las clases, el desarrollador decide:

- Qué categorías existen  
- Qué diferencia a una clase de otra  
- Qué tipo de predicciones podrá producir el sistema  

Estas decisiones influyen directamente en el comportamiento del clasificador.

### 📥 Definir los datos de entrada

En segundo lugar, implementar un clasificador implica decidir **qué datos se utilizarán como entrada**.

En el caso de imágenes:

- El sistema no recibe “objetos” ni “formas”
- Recibe **datos numéricos** que representan la imagen
- El clasificador siempre trabaja con datos, no con significados

Aunque estas transformaciones se ocultan en herramientas modernas, es importante tener presente esta idea.

### 🛠️ Usando Teachable Machine

Para explorar estos conceptos de manera concreta, en esta parte se utiliza **Teachable Machine**:  
https://teachablemachine.withgoogle.com/

Teachable Machine permite crear un clasificador de imágenes de forma visual, sin escribir código, lo que facilita observar el proceso completo. Con esta herramienta es posible:

- Definir explícitamente las **clases** 🏷️  
- Proporcionar **ejemplos** para cada clase 🖼️  
- Ver cómo el sistema empieza a **distinguir patrones** 🔍  

El objetivo de usar Teachable Machine **no es entrenar modelos**, sino **hacer visible qué significa implementar un clasificador como funcionalidad de software**.

### ⚠️ El clasificador puede fallar

Implementar un clasificador **no garantiza resultados correctos**. El sistema puede equivocarse, especialmente cuando:

- Las clases están mal definidas  
- Los ejemplos no representan bien el problema  
- Las clases se parecen demasiado entre sí  

Esto muestra que la calidad del clasificador depende directamente de las **decisiones de diseño** tomadas durante su implementación.
