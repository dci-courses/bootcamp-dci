---
format: html
toc: false
---

## Implementación de una funcionalidad de clasificación de imágenes 🖼️🤖

En esta sesión trabajamos en la **implementación y análisis de una funcionalidad de clasificación de imágenes** dentro de una aplicación de software. El foco está en **cómo una aplicación integra un modelo de inteligencia artificial y utiliza sus resultados**, no en entrenar modelos ni en estudiar teoría de inteligencia artificial.

El objetivo de la sesión es comprender cómo una funcionalidad basada en IA se incorpora a un sistema de software real, identificando sus componentes, su flujo de ejecución y las decisiones de diseño involucradas.

El trabajo de la sesión se organiza en **dos casos complementarios**, diseñados para comparar enfoques habituales en el desarrollo de software que integra modelos de aprendizaje automático ya existentes.

---

## Herramientas de trabajo

::: {.callout-note}
Durante toda la sesión se utiliza **Visual Studio Code (VS Code)** como editor de código.
:::

::: {.callout-warning}
Es obligatorio instalar la extensión **Live Server (Go Live)** en VS Code y ejecutar los ejemplos usando esta herramienta.

La extensión puede instalarse desde el **Marketplace de Visual Studio Code** en el siguiente enlace:  
[Live Server – Visual Studio Code Marketplace](https://marketplace.visualstudio.com/items?itemName=ritwickdey.LiveServer)

Alternativamente, la extensión puede buscarse directamente desde el **buscador de extensiones de VS Code**, escribiendo *Live Server*.
:::

---

## Estructura de la sesión

Durante la sesión se trabajan dos casos prácticos:

- **Caso 1**: implementación de una funcionalidad de clasificación de imágenes que utiliza un modelo entrenado en Teachable Machine y procesa video capturado desde la cámara.
- **Caso 2**: implementación de una funcionalidad de clasificación que utiliza un modelo preentrenado y procesa una imagen fija.

::: {.callout-tip}
Aunque ambos casos resuelven el mismo problema general —clasificar imágenes—, **parten de supuestos distintos sobre el modelo y la entrada de datos**. Esta diferencia es clave para entender decisiones de diseño en aplicaciones de software que incorporan IA.
:::

Cada caso se aborda por separado, analizando su funcionamiento, sus limitaciones y las implicancias que tiene el tipo de modelo utilizado en el comportamiento de la aplicación.

---

## Resultado esperado de la sesión

Al finalizar la sesión deberías ser capaz de describir cómo se implementa una funcionalidad de clasificación de imágenes dentro de una aplicación de software, identificar el rol que cumple el modelo de IA en el sistema y explicar las diferencias prácticas entre trabajar con modelos entrenados específicamente y modelos preentrenados reutilizables.
